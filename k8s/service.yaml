apiVersion: v1
kind: Service
metadata:
  name: medtracker-service
  namespace: default
  labels:
    app: medtracker
spec:
  type: LoadBalancer
  selector:
    app: medtracker
  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP
